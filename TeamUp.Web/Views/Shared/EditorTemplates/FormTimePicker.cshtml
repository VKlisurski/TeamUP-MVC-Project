@model DateTime?


<div class="row">
    <div class="col-md-3">
        @Html.LabelFor(g => g)
    </div>
    <div class="col-md-3">
        @(Html.Kendo().TimePickerFor(m => m))
    </div>
    <div class="col-md-3">
        @Html.ValidationMessageFor(g => g)
    </div>
</div>
